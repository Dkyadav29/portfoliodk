/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  // optional: include other settings
  reactStrictMode: true,
  // experimental: { appDir: true } // if you're using the app directory
};

module.exports = nextConfig;
